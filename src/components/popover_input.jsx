import React from 'react'

const PopoverInput = ({className = ''}) => {
  return (
    <div className={`popup-input position-fixed ${className}`}>PopoverInput</div>
  )
}

export default PopoverInput
